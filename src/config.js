// Optional public, read-only JSON feed. Never put AMP credentials in this file.
// Leave empty until a public feed is available; see readme.md for its contract.
window.FROSTBYTE_CONFIG = Object.freeze({ statusEndpoint: '' });
